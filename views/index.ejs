<!DOCTYPE html>
<html>
    <head>
        <title>Habit Tracker - Login</title>
        <link rel="stylesheet" href="main.css">
        <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    </head>
    <body>
        
        
        <div class="container1">
            <h1>Track Your Habits</h1>
            <h3>Healthy Habits Lead to Good Grades!</h3>            
            
            <p>Login with your email and password: </p>
            
            <form action="/login" method = "post">
                <div class="form-group">
                    <label for="usr_email">Email:</label>
                    <input class='input' type="text" class="form-control" name="email">
                </div>
                <div style="height:10px"></div>
                <div class="form-group">
                    <label for="pwd">Password:</label>
                    <input class='input' type="password" class="form-control" name="password">
                </div>
                <br>
                <input type="submit" value="Log in"></input>
            </form>
            
            <!-- show any messages that come back with authentication -->
            <% if (message.length > 0) { %>
                <div class="alert"><%= message %></div>
            <% } %>
            
            <br>
            <p>New user? Click below to signup!</p>
            <form class= "SignUp" action='/signup' method=get>
            <button>Sign up</button>
            </form>
        </div>
        
    
    </body>
    
</html>